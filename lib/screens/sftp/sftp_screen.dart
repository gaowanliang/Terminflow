import 'package:flutter/material.dart';

class SftpScreen extends StatelessWidget {
  const SftpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SftpScreen'),
      ),
    );
  }
}
