import 'package:flutter/material.dart';

class SnippetsScreen extends StatelessWidget {
  const SnippetsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('Snippets Screen'),
    );
  }
}

